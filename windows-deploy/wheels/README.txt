# Pre-downloaded Windows wheels for offline / fast install.
# Target: Windows x64 + Python 3.12 (matches install_python.ps1).
# Generated on Mac/Linux with: bash windows-deploy/download_wheels.sh
#
# setup.bat installs from this folder with:
#   pip install --no-index --find-links=wheels -r requirements.txt
#
# Do not delete large files such as pyside6_addons / pyside6_essentials.
