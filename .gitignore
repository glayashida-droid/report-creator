# Virtual environments
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# Local scratch outputs (tickets under .scratch/ stay tracked)
.scratch/*.docx
.scratch/**/output*

# Local project mirrors and saved sessions
data/

# Fingerprints for the公盘 → templates/ source mirror (the files themselves stay)
templates/.source_mirror.json

# Pre-downloaded Windows wheels (USB copy / regenerate with download_wheels.sh)
windows-deploy/wheels/*.whl
