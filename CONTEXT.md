# Report Creator

实验室检测报告的本地出具工具。试验照片与数据表附件以**合并视图**（本地正式路径 + 公盘备份）为列举真相；清单不进项目 JSON。

## Language

**试验目录**：
本地镜像 `data/{项目号}/3.测试组/` 下的一层文件夹，目录名为 `{Leg名}-{试验名}`（例：`Leg 1-温湿度试验`）。卡片上仍只显示试验名；目录名带所属 Leg 的 `leg_name` 前缀。不同 Leg 上同名试验各有各的目录。
_Avoid_: 测试中, 3.测试中, 与试验卡片完全同名, 同名试验共用目录

**挂钩**：
该试验因第一次建照片夹或添加数据表而在磁盘上创建试验目录之后的状态。挂钩后改卡片中文名则改目录名；目标目录已存在则改名失败并回滚。删已挂钩卡片或整条 Leg 经确认后删除对应目录；从未挂钩则直接删结构、不弹盘确认。
_Avoid_: 弱关联, 脱钩留盘

**同名试验（校验）**：
仅指同一条 Leg 内两张卡片的中文试验名相同。跨 Leg 允许显示名相同（目录靠 Leg 前缀区分）。
_Avoid_: 全项目试验名唯一

**照片文件夹**：
试验目录下一层子文件夹。模版四夹为试验前、试验中、试验后、数据；也可自定义名称。
_Avoid_: 相册, 未分类

**试验照片**：
试验明细中管理照片文件夹与缩略图的那一栏；经合并视图枚举，清单不写入项目 JSON。
_Avoid_: 公盘原图全量镜像, 测试组散图, JSON 照片清单

**全部重命名**：
对某一照片文件夹内全部图片按拍摄时间从 001 重新编号，前缀可选文件夹名、项目号或自定义。
_Avoid_: 按申请单号命名

**项目号**：
从项目文件夹名抠出的 `A` + 数字，作为本地镜像目录名，也用作照片「以单号命名」的前缀。
_Avoid_: 申请单号（用作照片前缀时）

**数据表附件**：
试验目录下固定子文件夹 `数据表附件/`，存放该试验挂上的 `.xlsx` 正文；与照片夹名「数据」不同。
_Avoid_: 数据（照片夹）, 内嵌表格编辑器

**数据表索引**：
试验节点上随项目 JSON 保存的清单（标题、路径、顺序），指向数据表附件里的文件；顺序即报告顺序。
_Avoid_: 把整表再存进 JSON

**编辑语言**：
主界面「项目定位」上的中文 | 英文总开关；只决定概览与明细当前编辑/核对哪一侧文案，两边分别保存。
_Avoid_: 中英文叠加编辑, 导出语言

**导出语言**：
生成 Word 时三选一：中文、英文、中英文；分别对应 `template_zh` / `template_en` / `template_ze`。中英文是导出时拼装，不是第三种编辑态。
_Avoid_: 编辑语言, 模板未就绪

**目录骨架**：
本地 `data/{项目号}/` 下与公盘同构的文件夹树；夜间同步清掉已上传的大文件后仍保留空目录，供白天新照片/数据表写入。
_Avoid_: 删整个项目夹, 全量镜像回灌

**合并视图**：
列表、预览、导出枚举照片/附件时，合并本地正式路径与公盘同相对路径；本地有文件优先，仅公盘有则带云标记；不含 `备用/`。
_Avoid_: 双份拷贝, 纯扫本地

**云端照片**：
仅存在于公盘、本地正式相册无原文件的图片；界面用缩略图（可缓存在 `.thumbs`）+ 云标记表示；可显式「下载到本地」写入正式相册。
_Avoid_: 预览自动落盘, 散图扫描

**备用**：
试验目录 `3.测试组/{Leg}-{试验}/备用/` 下存放从正式相册移入的已删照片；正式相册列表与导出排除；试验照片栏文件夹预览末尾可见，可拖回正式相册还原；随夜间同步上公盘。
_Avoid_: 软删除 JSON 清单, 永久硬删

**夜间同步**：
程序运行到设定时刻，将本地工作区相对公盘的新增/变更大文件增量上传；校验一致后删本地原文件，保留目录骨架；`project_state.json` 不在此流程（保存明细时即时上公盘；公盘不可写则先落本地并待同步）。
_Avoid_: 系统 cron, 实时双向同步

**待同步**：
公盘不可写时保存明细仍写入本地 `project_state.json` 并留下标记；公盘恢复后若期间无更新则补传，若公盘已有更新则询问覆盖方向，不静默覆盖。
_Avoid_: 断连不落盘, 静默覆盖公盘
