# Report Creator 产品需求文档 (Spec)

## 1. 问题陈述 (Problem Statement)
当前测试实验室在完成样品接收、测试分配并产生结果后，需要出具结构化的 Word 格式检测报告。人工填写报告的过程繁琐、容易遗漏、且高度重复。报告内容涉及大量的从申请单（Excel）、报价单（PDF）、以及基础数据（标准库、设备库）的搬运与映射。为了提高实验室出具报告的效率与准确性，需要开发一款自动出具报告的桌面客户端工具，实现“一键输出报告”的核心诉求。

## 2. 解决方案 (Solution)
开发基于 **PySide6** 的桌面应用（Report Creator），具备以下核心能力：
1. **自动提取信息**：解析 Excel 格式的申请单获取客户和样品信息；解析 PDF 格式的报价单提取试验项目作为报价候选池。
2. **可视化 Leg 图排布**：以网格或流线图的形式，可视化排布并行的样品组测试路径（Leg）和每个 Leg 下的串行试验。不同 Leg 之间可以包含相同或不同的试验。
3. **数据映射与录入**：对接本地《标准库》与《设备库》，通过界面下拉框进行关联；选中标准后若库中有「关键参数」，须人工确认（可改）后再写入检测条件；提供界面录入样品编号与测试结果（Pass/Fail）。
4. **状态持久化**：支持将当前编辑的排布与明细信息保存为本地 JSON 项目文件，以便随时重新加载。
5. **模板化报告生成**：基于 `python-docx`，读取包含占位符的 Word 模板（`template.docx`），动态填充文本和扩展表格，自动将各项目的测试图片（按限制的长宽比例）附入报告，输出最终的 Word 检测报告。
6. **Leg 模板库**：支持将当前 Leg 布局（含试验顺序、试验名称、所选标准身份）保存为可跨项目复用的模板；新项目可直接导入，导入时从活的标准库灌入检测条件/结果描述/评价要求，关键参数保持未确认。模板不含设备、样品编号、结论、日期，也不含已确认的关键参数或替换后的条件正文。报价候选池与模板池彼此独立：导入不改写报价池、也不做名称对齐或替换。左侧候选区用「报价单 / 模板」开关切换显示；Leg 下拉框从两份名单的并集中挑选。

## 3. 用户故事 (User Stories)
1. **US-01 导入项目**：作为报告员，我可以在主界面输入项目号，程序能自动匹配并加载公盘中对应的项目文件夹。
2. **US-02 确认申请单**：作为报告员，程序能自动解析该项目的 Excel 申请单，并在界面展示客户信息和样品信息供我人工核对。
3. **US-03 构建候选池**：作为报告员，程序能自动解析项目的 PDF 报价单，将其中的试验项目提取并列在侧边栏「报价单」候选池中，方便我后续分配。该池只反映本次报价单，不被模板导入改写。
4. **US-04 绘制 Leg 图**：作为报告员，我可以通过点击 `+ Leg` 和 `+ Test` 按钮，绘制横向并行、纵向串行的测试流程图，并通过下拉框给每个 Test 节点绑定具体的试验。下拉数据来自报价池与模板池的并集（字面完全相同的名字在下拉里只出现一次）。选中后仍可在框内手工改名；改名只作用于该节点，不写入报价池或模板池。候选池是挑选来源，不是闭集：Leg 图中的试验可以多于当前报价单。
5. **US-05 填写试验明细**：作为报告员，我点击任意一个试验节点，可以弹出明细界面，在其中设置试验开始/结束日期（受全局日期限制）、选择标准和设备、录入应用的样品编号（如 A01~A05），并为样品选择 Pass/Fail 的结论。选中标准后，检测条件先载入标准库原文；若该行「关键参数」非空，条件抽屉上出现对应输入框（逗号分隔，几个参数几个框）和确认勾选。勾选后用当前输入值从原文替换关键参数并写入条件正文；取消勾选则条件回到原文，输入框保留。无关键参数的标准不需确认。标准、关键参数（如有）、设备、样品都填好后，主界面该试验右侧出现完成勾；缺任一项则不出现。
6. **US-06 保存与加载项目**：作为报告员，我可以通过“保存”按钮将当前排布的 Leg 图及所有填写的明细数据保存下来；下次打开工具时可以重新加载这些状态继续工作。项目 JSON 保留已确认的关键参数与替换后的检测条件。
7. **US-07 生成报告**：作为报告员，我可以选择“生成单项”、“生成单条 Leg”或“生成所有 Leg”来一键导出 Word 报告，且报告中的照片要求自动从对应测试目录中抓取并按原长宽比等比排版。导出范围内任一试验没有完成勾（含关键参数未确认）时，拦截导出并列出未完成项，不生成文件。
8. **US-08 保存 Leg 模板**：作为报告员，我可以将当前 Leg 图保存为跨项目模板。保存时弹出窗口由我命名；若本次图是刚从某模板导入的，输入框预填该模板名。模板写入应用级目录 `data/leg_templates/`，不写入该项目的镜像文件夹。模板包含布局、试验名和标准身份，不包含设备、样品、结论、日期，也不包含关键参数确认状态或替换后的条件正文。同名已存在时需确认覆盖。保存后的图仍可继续改，再存为新模板或覆盖原模板。
9. **US-09 导入 Leg 模板**：作为报告员，我可以从已保存的模板列表中选一份导入到当前项目。导入只替换 Leg 图，不改申请单、日期和报价池。每个试验框自带模板中的名称，仍可增删改。导入后从活的标准库灌入检测条件/结果描述/评价要求，关键参数回到库默认且未确认，因此完成勾不会立刻出现，须逐条进明细确认。导入后刷新模板池为该模板中的试验名（按出现顺序去重）；左侧切到「模板」即可查看，下拉框可同时选报价池与模板池中的名字。不做模糊匹配，不把模板名改写成报价单用词。导入前若当前已有 Leg 图，需确认覆盖。

## 4. 实现决策 (Implementation Decisions)
*   **UI 框架**：采用 `PySide6` 开发，利用 Qt 的 Graphics View 框架或动态生成的嵌套 Layouts 来实现 Leg 图的绘制交互。
*   **依赖库**：
    *   Excel 解析：复用已有的 `application_parser` 包及 `openpyxl` / `pandas` (用于标准库与设备库读取)。
    *   PDF 解析：使用 `pdfplumber` 提取报价单文本内容。
    *   Word 生成：使用 `python-docx` 进行模板渲染、占位符替换和动态表格生成。
*   **持久化格式**：使用标准 JSON 格式在本地记录项目的上下文状态 (Project State)。
*   **报告映射逻辑**：
    *   基础数据（客户、样品）：映射到首页与汇总表。
    *   试验明细数据：映射到明细章节，其中检测条件、评价要求等自动根据选择的标准号+章节号去匹配“标准库.xlsx”的内容。
    *   关键参数：读标准库「关键参数」列，按 `,` / `，` 拆分。选中标准时条件显示库原文；用户确认后，从库原文把默认关键参数替换成输入值，只改检测条件（标准描述），不改评价要求与结果描述。每次确认都从库原文替换，不在已替换稿上再替一次。
*   **完成勾与导出拦截**：试验卡片右侧完成勾要求标准、设备、样品均已填，且每条已选标准若有关键参数则必须已确认。导出按当前范围检查（全部 / 单条 Leg / 单项），范围内有未打勾的试验则拒绝导出。
*   **双候选池**：`candidate_pool` 仅来自报价单解析；`template_pool` 仅来自最近一次导入的 Leg 模板（试验名按出现顺序去重）。两池互不写入。左侧用「报价单 / 模板」开关切换显示。Leg 下拉框使用两池并集，仅对字面完全相同的字符串去重；当前节点已绑定但不在任一池中的名字仍须能显示。
*   **Leg 模板**：独立 JSON，存放在 `data/leg_templates/`（与项目镜像平级），不以项目号为目录。模板名由保存弹窗人工指定，作为显示名与文件名（非法路径字符替换为 `_`）。
*   **模板载荷**：`legs` 及其节点上的 `test_name` 与标准身份（`standard_id` / `standard_chapter` / `standard_test_name`）。明确剥离库灌入的正文（`standard_desc` / `result_desc` / `evaluation_req`）、关键参数及其确认状态、`equipments` / `equipment_name`、`samples`、起止日期。导入时卡片保留模板原名，再从活库灌入正文；关键参数未确认。
*   **报告生成**：只认 Leg 图，不校验试验是否来自原始报价单；但导出前校验范围内试验是否已打完成勾。

## 5. 测试接缝与决策 (Testing Decisions)
*   **UI 与逻辑解耦 (Seam 1)**：核心的“解析 PDF”、“解析 Excel 申请单”、“读取标准库/设备库”以及“生成 Word 报告”的逻辑必须与 GUI 代码分离。这允许我们在没有界面的情况下，使用示例数据（如 `example/A2260613686101`）对解析和报告生成进行独立的单元测试或脚本联调。
*   **状态模型 (Seam 2)**：项目状态（Leg 结构、测试节点数据）抽象为一个独立的 Python Data Class / Pydantic 模型，确保 Save/Load 功能的确定性。
*   **Leg 模板 IO (Seam 3)**：保存/列出/加载模板与 GUI 分离。对一份不含设备与样品的 `legs` JSON 做读写测试；验证导入后报价池不变、模板池为模板试验名、下拉并集仅对字面相同名去重、不做相似名替换；导入灌库后关键参数为库默认且未确认。
*   **关键参数替换 (Seam 4)**：拆分与替换是无 UI 的纯函数，从库原文 + 默认参数 + 用户输入计算条件正文；覆盖空列、1/2/3 个参数、70→85 取消再 90、原文找不到参数等情形。
*   **完成标准 ("Done" Criteria)**：程序能够走通从载入真实的 `A2260613686101` 项目文件夹开始，到渲染出与用户预期结构相符（含对应试验照片、表格扩展正确）的 Word 报告。

## 6. 范围外说明 (Out-of-scope Items)
*   不包含线上多人协同功能，当前仅作为单机本地客户端运行。
*   暂不支持除 Excel 和特定 PDF 以外的其他格式报价单 / 申请单解析。
*   暂不涉及对 Word 模板本身复杂的样式定制（如动态调整字体、颜色等，依赖现有模板的样式设定）。
*   暂不支持模板云同步、多人共享或模板版本树；覆盖同名即为更新。
*   暂不做试验名模糊匹配或自动改名（例如「随机振动」与「随机振动试验」视为两项，由报告员在下拉里自行选择）。

## 7. 进一步的备注 (Further Notes)
*   在开发“标准库”与“设备库”的下拉关联逻辑时，需考虑数据的索引效率，因库文件可能会随着时间增长。
*   照片插入模块需重点处理：如果原图片比例不符合模板的预期，应“优先满足宽度，但不能破坏原有比例，不得变形压缩”。
